import { StyleSheet} from 'react-native';

const styles = {
    container: {
        height: 50,
        width: 100
    },
    button: {
        borderColor: 'purple',
        borderWidth: 4,
        backgroundColor: 'red',
        alignItems: 'center',
        borderRadius: 2,
    },
    buttonText: {
     color: 'white',
    }
}

export default styles;