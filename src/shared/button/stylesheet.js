import { StyleSheet} from 'react-native';

const styles = {
    container: {
        height: 45,
    },
    button: {
        flex: 1,
        backgroundColor: 'blue',
        borderWidth: 2,
        alignItems: 'center',
        justifycontent: 'center',
        borderRadius: 2,
    },
    buttonText: {
     color: 'white',
    }
}

export default styles;